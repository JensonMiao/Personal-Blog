<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miaojingu.blog.article.mapper.CategoryMapper">

    <resultMap id="categoryLabelMap" type="Category">
        <id column="id" property="id"></id>
        <result column="name" property="name" />
        <collection property="labelList" javaType="list" ofType="Label">
            <id column="label_id" property="id"/>
            <result column="label_name" property="name"/>
        </collection>
    </resultMap>

<!--    查询正常状态的分类及分类下的所有标签-->
    <select id="findCategoryAndLabel" resultMap="categoryLabelMap">
select m1.id,m1.name,m2.id label_id,m2.name label_name from mjg_category m1 left join mjg_label m2 on m1.id=m2.category_id where m1.status=1 order by m1.sort
    </select>
</mapper>
